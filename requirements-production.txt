# Optional production retrieval backends. Only needed when ONTO_ES_ENABLED /
# ONTO_NEO4J_ENABLED / ONTO_MILVUS_ENABLED are set - the default SQLite-backed
# demo tier (see README.md / docs/FULL_PRODUCTION_RUN.md) needs none of this.
elasticsearch>=8.0
neo4j>=5.0
pymilvus>=2.4
